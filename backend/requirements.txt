# 🔷 FastAPI API Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
requests==2.31.0
python-multipart==0.0.6

# 🔷 Google Gemini AI Integration
google-generativeai==0.8.5

# 🔷 Web Scraping / Content Gathering
beautifulsoup4==4.12.2
lxml==4.9.3
aiohttp==3.9.1
tldextract==5.1.1

# 🔷 NLP + Preprocessing
python-dotenv==1.0.0
PyYAML==6.0.1

# 🔷 Database & Storage
# sqlite3 is built-in with Python

# 🔷 CORS and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.8.0

# 🔷 Async File Operations
aiofiles==24.1.0

# 🔷 Development & Logging
python-json-logger==2.0.7

# (Optional) Local Backend (editable install) — install separately if needed
# -e .

# 🔷 Optional Utilities
mlflow           # (Optional) experiment tracking
pandas

python-box==6.0.2  # For configuration management
ensure==1.0.4  # For ensuring package versions
tqdm  # For progress bars
joblib # For parallel processing and caching

dvc # Data Version Control
dvc[gdrive]  # or s3, depending on remote
matplotlib
seaborn
# Already included above as editable install
# pip install -r requirements.txt
